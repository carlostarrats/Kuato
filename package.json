{
  "name": "kuato",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "kuato": "./dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "hooks",
    "skill"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/build-overlay.mjs",
    "daemon": "node dist/daemon/main.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@types/node": "^22.19.20",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
